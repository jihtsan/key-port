# KeyPort 一期实施追踪

- 关联实现 Issue：[#64](https://github.com/jihtsan/key-port/issues/64)
- 产品范围基线：[一期产品边界与功能整合规划](./一期产品边界与功能整合规划.md)
- 基线提交：`32a64d5`（`main`，已合并 PR #63）
- 追踪状态：阶段 1–6 已完成；真实 CloudKit/签名/多机联调和原生 UI 工具证据仍是外部验收项

本文是实现追踪，不是产品规划的替代版本。每个状态都以当前源码、测试和可复核的运行证据为准；历史 Review、旧计划和枚举名称不视为功能已交付。

## 当前事实源与兼容层

| 主题 | 当前实现 | 一期处理口径 |
| --- | --- | --- |
| 展示与拓扑事实 | `TopologySnapshot` 是 Graph 与统一拓扑的事实源；`TopologyStore` 保存本地拓扑 | 继续复用，不创建第二套 Graph/路由事实源 |
| SSH 兼容投影 | `AppSnapshot`、`ServerConnection` 和 `SSHCompatProjection` 仍承载旧 SSH/Keychain 适配 | 逐步将新流程接到拓扑事实；保留可回滚的兼容投影，直到迁移门槛满足 |
| 账户与路径 | `SSHAccount` 归属于 Node；`SSHConnectionProfile` 引用账户并保存别名与 `SSHRoutePolicy` | 授权继续账户级；路径扩展为固定端点或有序允许候选端点 |
| 当前路径策略 | `SSHRoutePolicy` 保留 `automatic(networkScope:)` 与 `fixed(endpointID:)`；`SSHConnectionProfile.candidateEndpointIDs` 保存可选的有序候选 | 空候选继续按本机观测、`Endpoint.priority` 和稳定 ID 排序；非空候选严格按用户顺序且只允许同 Node 的节点级 SSH 端点 |
| TCP 地址选择 | `AddressSelectionCoordinator` 已有固定不自动回退、并发探测、整体预算、WaitingForUser 与网络 epoch，但 `AddressSelectionV2Gate.defaultEnabled` 为关闭 | 先接入真实 SSH 计划与辅助脚本；保留固定策略和“接通后不切换”门禁 |
| SSH 配置 | `SSHConfigService` 原子生成 `~/.ssh/keyport/config`，并幂等加入 `Include ~/.ssh/keyport/config`；已有用户配置不整体覆盖 | 继续保护用户 `~/.ssh/config`、现有别名和受限权限；新增脚本配置与生成状态必须可诊断、可修复 |
| 身份与授权 | `HostKeyService`、`OpenSSHService`、`TrustedSSHSession` 使用系统 `/usr/bin/ssh`；Keychain、AskPass FIFO、公钥安装/读取/精确撤销已存在 | 不内嵌 SSH、不让辅助脚本读取密码；多地址只复用已确认的账户授权，不重复安装公钥 |
| 同步与迁移 | `TopologyCloudMetadataSnapshotPolicy` 清洗本地观测、私钥路径和本机状态；Host v6 CloudKit v2/authority/shadow migration 仍是恢复契约 | 同步 profile、端点和授权元数据时沿用显式 allow-list、冲突、版本、墓碑和本地恢复规则 |
| 隧道承重能力 | `KeyPortTunnelBroker`、`TunnelRegistry` 和既有 C4 回归仍在仓库中 | 不因一期导航整合删除；服务隧道暂不扩展为一期主流程 |

## 旧数据、别名与生成文件迁移

| 旧对象/能力 | 新一期归属 | 兼容约束 |
| --- | --- | --- |
| `AppSnapshot.servers[].id` | `SSHConnectionProfile.id` | 保留 profile ID，避免 Keychain、授权和审计目标漂移 |
| `ServerConnection.name/group/notes` | `Node.name/group/notes` | 同一 Node 下多个账户/端点共享机器信息，不按用户名或公网 IP 自动合并 |
| `ServerConnection.host/port` | `Endpoint.address/port/protocol/networkScope` | 旧记录迁移为一个固定端点；端口不同仍可能是不同机器，必须有稳定身份或人工确认 |
| `ServerConnection.username` | `SSHAccount.username` | 账户稳定身份为 `(Node, normalized username)`；同一账户可被多个 profile 引用 |
| `ServerConnection.alias` | `SSHConnectionProfile.sshAlias` | 原有命令继续有效；固定策略不会因升级自动变为候选回退 |
| 旧 `Authorization` | `SSHAuthorization(accountID, keyID, fingerprint)` | 共享授权事实归账户；`AccessVerification` 仅记录某设备最近一次验证 |
| `ServerConnection.status/keyCheck/lastCheckedAt` | 当前设备的 `AccessVerification` | 不把云端同步成功或远端授权存在伪装成本机可登录 |
| `~/.ssh/keyport/config` | 本机生成的 OpenSSH 配置 | 原子写入、`0600`、冲突 fail closed；用户 `~/.ssh/config` 只做幂等 Include |
| `~/.ssh/keyport/known_hosts` | 本机端点 Host Key 信任材料 | 每个候选端点独立核对映射；不关闭 `StrictHostKeyChecking`，不盲目信任扫描结果 |
| `~/.ssh/keyport/identities/` | 当前设备本地密钥 | 私钥不进入 CloudKit、归档或脚本配置；每台 Mac 独立生成/持有 |
| 旧 Servers/Keys/Devices/Logs 页面 | 服务器、我的设备、活动、设置 | 重复入口已从一级导航退役；独有能力已迁移，不删除用户数据、安全门禁或活动记录 |

## 状态语义锁定

一期的三个状态轴必须独立显示，不能用一个“已同步”或“在线”概括：

1. **配置同步**：profile、端点、别名及非敏感工作区资料是否已与 iCloud 合并；另外显示本机 SSH 派生配置/辅助脚本是否存在且与当前事实一致。
2. **授权事实**：指定 `SSHAccount` 是否接受指定 `WorkspaceDeviceProfile` 的 `SSHKey`，由账户级 `SSHAuthorization` 的远端状态表达。它不表示当前网络可达，也不表示本机最近登录成功。
3. **最近验证**：当前设备依照具体 `SSHConnectionProfile + Endpoint` 的 `AccessVerification`，必须带设备、时间、端点、传输方式和结果；其他设备只能显示最近已知资料，不冒充实时状态。

`ReachabilityObservation` 另行表示有时间和网络 epoch 的本机 TCP 可达性。主机身份信任、远端授权、可达性和最近验证互不替代。

## D1–D9 对照

| 编号 | 基线观察 | 状态 | 后续验收 |
| --- | --- | --- | --- |
| D1 | Graph 与“节点”并列；“全部节点”是节点页内筛选 | 已完成，阶段 5 | 一个“服务器”入口，列表/图谱切换；全部只是筛选 |
| D2 | 侧边栏仍有独立“SSH 账户”入口 | 已完成，阶段 5 | 账户编辑、授权读取/撤销和首次接入均在服务器详情中，一级导航不再重复 |
| D3 | 活动与审计日志仍是两个入口 | 已完成，阶段 5 | 合并为活动，提供进行中/失败/全部筛选与结构化审计详情 |
| D4 | 密钥页仍混合连接导入、密码和本机密钥 | 已完成，阶段 5 | SSH Config 发现连接迁入服务器入口；本机密钥和高级管理放入我的设备 |
| D5 | 设备页通过 `DevicePresenceMerger` 混合工作区 Mac 与 Tailscale 发现节点 | 已完成，阶段 5 | 我的设备只展示已登记 KeyPort 设备；Tailscale 发现放入服务器发现区 |
| D6 | Graph 检查器、节点详情和旧详情有重复操作 | 已完成，阶段 5 | 服务器工作区集中列表主操作；图谱主节点使用服务器详情，检查器保留为关联对象高级信息 |
| D7 | Graph 与节点工作区是两个侧边栏入口 | 已完成，阶段 5 | 服务器工作区内共享列表/图谱的选择、搜索和操作 |
| D8 | 页面仍出现 `Test Case`、`V6`、兼容适配器等工程文案 | 已完成，阶段 5 | 产品层改为服务器关联、只读状态和诊断；旧内部枚举仅保留在实现层 |
| D9 | 机器配置在详情中占据独立高注意力区域，按钮写“同步” | 已完成，阶段 5 | 下沉为服务器信息，按钮改为“刷新信息”；不扩展性能监控 |

## O1–O11 对照

| 编号 | 当前证据 | 状态 |
| --- | --- | --- |
| O1 | `SSHFirstAccessState` 记录主机身份、凭据、本机密钥、授权写入和公钥复检边界；详情页提供连续进度 | 已完成，阶段 4 |
| O2 | Node、Endpoint、SSHAccount 已分离；Tailscale 有精确匹配与人工关联门禁 | 部分完成，阶段 2/4 |
| O3 | `SSHAccessSetupView` 可切换固定/自动；自动策略可保留旧的全量候选，也可勾选并上下调整有序候选，数据写入 profile | 已完成，阶段 2 |
| O4 | `NodeWorkspaceHeader` 根据固定、自动候选和手选状态显示“固定 / 自动 / 候选 N / 手选” | 已完成，阶段 2 |
| O5 | `KeyPortSSHRelay` 以独立可执行目标交付；路由清单、版本/权限检查、原子修复、超时/总预算和 OpenSSH 独立 fixture 已覆盖 | 已完成，阶段 3 |
| O6 | 当前设备页显示本机密钥、待授权账户、一次本机身份确认和可恢复的批量授权结果 | 已完成，阶段 4 |
| O7 | Cloud policy 与加密归档均保留候选顺序，并继续清洗本机密钥路径、观测和审计字段；页面状态已拆分 | 已完成，阶段 2/5 |
| O8 | 当前设备页支持目标选择；批量计划逐项记录成功/失败/等待处理，支持取消、暂停恢复和只重试失败项 | 已完成，阶段 4 |
| O9 | `PasswordlessPrimaryAction` 已按状态切换；服务器详情承载首次接入与连接配置主操作 | 已完成，阶段 5 |
| O10 | Graph 与服务器列表共用 `NodeWorkspacePresentation`；账户、端点、别名、授权验证状态和异常筛选可追溯到同一 Node | 已完成，阶段 5/6 |
| O11 | 旧 alias 和固定 profile 可迁移，已有 Include 受保护；有序候选与连接前回退 helper 已交付，固定模式仍不自动换地址 | 已完成，阶段 2/3 |

## 阶段与验收门槛

| 阶段 | 交付 | 状态 |
| --- | --- | --- |
| 1 | 基线、Issue、兼容清单、状态语义和本追踪文档 | 已完成 |
| 2 | 有序候选/固定端点模型、编辑器、schema/归档/迁移/冲突测试 | 已完成 |
| 3 | 通用辅助脚本与配置、OpenSSH 独立连接、依赖检查/修复、端到端本地 fixture | 已完成；PR #69 |
| 4 | 首次接入、跨 Mac 授权状态、批量进度与失败恢复 | 已完成；真实 CloudKit/Keychain/签名联调是外部验收项 |
| 5 | 服务器/我的设备/活动/设置导航整合，迁移旧页面独有能力，产品文案清理 | 已完成；PR #71 |
| 6 | Graph/列表共用事实与操作，完整回归、release/启动和原生 UI 检查记录，最终验收报告 | 已完成；PR #73 |

## 基线验证

以下命令在 `32a64d5`、干净工作树、当前 macOS 环境执行：

- `swift --version`：Swift 6.4.0，arm64，macOS 26 SDK。
- `swift test`：退出码 0；测试清单共 465 项，当前运行输出包含既有 CoreLocation 弃用提示和测试夹具中的 Swift 6 并发警告。
- 已确认仓库规定的 `./script/test.sh`、`./script/test_c4.sh`、release build、bundle 启动和 diff check 作为后续按阶段执行的验证入口；阶段 1 未把它们的未执行状态伪装成通过。

## 阶段 2 验证

- `swift test --filter SSHConnectionPlanningTests`：19 项通过；覆盖有序候选、旧 payload 默认值、死引用/跨 Node/非 SSH/网络范围冲突、迁移、Cloud policy 和加密归档。
- `swift test --filter UnifiedTopologyAppModelTests`：7 项通过；覆盖自动候选顺序从编辑器保存到 `TopologyStore` 的持久化。
- 现有 Swift 6 并发和 CoreLocation 弃用提示仍存在于既有代码/测试夹具中，本阶段未扩大其范围。

## 阶段 3 验证

- `swift test --filter SSHPreconnectRelayTests`：4 项通过；覆盖有序候选、目标匹配、IPv6 主机、固定/旧自动策略兼容、配置校验和敏感字段边界。
- `swift test --filter SSHConfigServiceTests`：3 项通过；覆盖 owner-only helper/manifest、ProxyCommand、健康状态、缺失依赖时保留旧配置和可修复依赖安装。
- `./script/test_ssh_relay.sh`：通过；覆盖 helper 版本、IPv4 首选失败后的备用地址、双向字节转发、接通后不再尝试第三地址、单候选超时/总预算、IPv6，以及系统 `/usr/bin/ssh` + 临时 `sshd` 的认证与 Host Key 校验。
- 连接前回退只为显式有序候选自动策略生成清单；固定策略和没有显式候选的旧自动策略继续使用原有 direct/Tailscale transport。helper 在 OpenSSH 接管连接后不执行认证重试或会话迁移。
- 本地应用 bundle 的 `Contents/Helpers/KeyPortSSHRelay` 已纳入构建、复制、权限检查和签名流程；主应用退出后生成的 alias 不依赖主应用常驻。

## 阶段 4 验证

- `swift test --filter SSHAuthorizationWorkflowTests`：9 项通过；覆盖首次接入状态机、写入后复检边界、Host Key 阻断、密码介入、取消/过期终态、批量只重试失败项、进程中断恢复、敏感字段排除和跨设备授权投影。
- `swift test --filter PasswordlessPrimaryActionTests`：6 项通过；覆盖已写入待复检状态只触发公钥检查，不重复安装远端公钥。
- `swift test --filter TopologyGraphProjectorTests`：4 项通过；确认新增状态不会破坏 Graph 的授权、验证和安全阻断投影。
- 批量计划只在本机 `UserDefaults` 保存稳定目标 ID、阶段、尝试次数和失败分类；密码、私钥路径、`known_hosts` 和 SSH Agent 状态不进入计划或跨 Mac 投影。

## 阶段 5 验证

- `swift test --filter NavigationIntegrationTests`：3 项通过；确认一级导航只暴露服务器、我的设备、活动，活动失败筛选可识别结构化失败记录，Tailscale-only 发现不进入设备工作区。
- 服务器工作区新增列表/图谱切换，共享 `AppModel.selectedServerID`、搜索文本和图谱主节点选择；“全部服务器”和标签入口只切换服务器列表筛选。
- SSH Config 发现连接和 Tailscale 发现节点均可在服务器入口处理；设备入口只保留已登记 KeyPort 设备、设备密钥、高级密钥管理和本机批量授权。
- 活动入口统一进行中、失败、全部历史和结构化审计详情；活动记录继续遵守秘密隔离约束。
- 服务器详情继续承载首次接入、账户/路径编辑、远端授权读取/精确撤销和机器信息刷新；旧一级入口未删除数据或安全能力。

## 阶段 6 验证

- `swift test --filter ServerWorkspaceProjectionTests`：1 项通过；覆盖同一 Node 的多个 SSH 账户、多个地址、账户/地址搜索、无账户节点和异常筛选。
- `swift test`：KeyPort 191 项中 190 项通过，唯一失败是仓库既有的 `ProcessExecutorTests.testExitStatusAndSeparatedOutputAreCaptured` 时序断言；KeyPortCore 304/304 通过。单独重跑 `swift test --filter ProcessExecutorTests` 后 10/10 通过。
- `swift run KeyPortCoreChecks`、`swift build --product KeyPortAskPass`、`swift build --product KeyPortSSHRelay`、`./script/test_ssh_relay.sh` 和 AskPass FIFO 校验均通过。
- `./script/test_c4.sh`：安全跳过；本机为 `OpenSSH_10.3p1`，不在仓库允许的 C4 fixture 版本范围内。
- `swift build -c release --product KeyPort`：通过；仅保留既有 CoreLocation 弃用提示。
- `./script/build_and_run.sh --verify`：通过；从当前工作树构建、签名并启动 `dist/KeyPort.app`，本地验证采用 ad-hoc 签名且关闭 iCloud entitlement。
- 服务器列表改为消费与 Graph 相同的 `NodeWorkspacePresentation.serverItems`；列表/图谱共享搜索、选中对象和“仅异常”筛选，账户、地址、配置编辑、添加账户和现有安全门禁继续走 `AppModel` 既有入口。
- 列表选择账户会同步到同一 Node 详情；只有 Node 或 SSH 用户、尚未建立连接配置的服务器也可从列表进入同一详情并继续添加账户/地址。
- 已尝试使用原生 UI 工具读取启动后的窗口，但工具返回 `Sky Computer Use native pipe closed before response`；因此本轮记录了 bundle 启动证据，视觉、键盘和无障碍证据仍按外部验收项保留，未将源码或启动结果冒充视觉验收。

## 一期最终验收

- 阶段 1–6 均已通过独立 Issue、分支和关联 PR 合并到 `main`；阶段 6 为 Issue [#72](https://github.com/jihtsan/key-port/issues/72) / PR [#73](https://github.com/jihtsan/key-port/pull/73)，squash merge 提交为 `92aa3066ad4654f1e415c5451fa1dd9739cbec81`。
- 一期已交付服务器/我的设备/活动导航整合、首次接入和失败恢复、候选地址与独立 SSH relay、跨设备元数据投影、服务器列表/图谱事实闭环；不删除用户数据、私钥、Keychain、SSH 配置或现有安全门禁。
- 仍需真实环境验收的边界：两台 Mac 的 CloudKit/iCloud Keychain、团队签名 entitlement、真实多地址 Host Key 映射与真实 SSH 会话；这些不能由本地 XCTest、ad-hoc bundle 或内存同步替代。

## 未决的外部验收

- 两台真实 Mac 的 CloudKit/iCloud Keychain、团队签名 entitlement 和跨设备字段清洗需要真实环境；本地 XCTest/内存同步不能替代。
- 两地址同一账户的真实 Host Key 映射、连接前回退、主应用退出后 `ssh <别名>` 以及精确撤销，需要隔离的本地 `sshd` 或明确授权的测试服务器。
- Stage 3 的本地 OpenSSH fixture 已证明“首选地址关闭、备用地址接通”时 helper 与系统 SSH 的连接前闭环；多个真实地址的 Host Key 是否代表同一目标仍需按端点逐一确认，不能由 TCP 接通推断。
- 原生窗口、键盘和无障碍证据：本轮原生 UI 工具连接失败，已记录错误和 bundle 启动结果；源码或 bundle 启动不等于视觉验收，需在工具可用时补做。
