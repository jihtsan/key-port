# Issue #92 首次访问连续流程验收

**结论：待用户验收。** 功能与隔离边界已自验；没有将测试或模拟结果作为真实 SSH/视觉验收通过。前置 #90 / PR #91 仍为草稿、待用户视觉确认，本阶段亦不合并，不关闭 #78/#90/#92，不进入 Graph 或真实服务接入。

用户在 #92 明确授权开展依赖 #91 的第二阶段隔离预览，这不是主页原尺度像素一致的证明。完整状态转移、模拟入口和后续迁移边界见 [state-transitions.md](state-transitions.md)。

## 复核修复：6250f76（当前版本）

当前构建源码为 `6250f76`，仍待视觉验收，不合并 PR #93。下文 `6db2877` 的 37 组证据保留为上一版完整流程基线，不能当作当前二进制截图。当前补充证据见 [evidence/6250f76](evidence/6250f76)，覆盖本轮受影响路径。

- 修复：中文、emoji、符号名称不能生成空 SSH 目标。可读英文名称保持原有规则；转换结果为空时，使用标准化名称 SHA-256 前 12 位十六进制生成 `server-…`。这是稳定默认值，不宣称生产别名全局唯一；后续真实配置写入仍需处理冲突。
- 提交时固定最终别名；显式别名优先。成功页和终端/复制适配接口共用相同目标；重试或修改地址不改变已确定别名。
- 实际 App 回归：名称“我的服务器”、高级别名留空，提示自动值 `server-3f0f164b30c3`；使用示例密码完成模拟流程，成功页显示 `ssh server-3f0f164b30c3`。截图及完整 AX 已保存。
- 取消页去掉正文中重复的授权状态句；真实授权中按 Escape 后，状态只在副标题出现一次，仍明确取消不代表撤销。
- `swift test --filter KeyPortInterfaceTests`：19/19，通过新增非 ASCII 默认值、显式覆盖及两个交接适配接口目标一致性回归；构建、严格签名验证和真实 App 启动通过。未执行真实 SSH、剪贴板、钥匙串或云操作。
- 主机确认承载方式、新增错误页和整体视觉依然待用户确认，本轮没有将这些项标记通过。

## 上一版完整流程基线：构建与复现

- 分支：`codex/issue-92-first-access-flow`。fetch 后从 PR #91 最新 head `cece27fab25ae3c0103e19f661f13e2b8de96f1c` 建立；交付前重查 #91 head 未改变。
- **上一版基线源码 commit：`6db2877ba99949b454c1bd1295fd03eaf9fdca6b`**。后续仅补证据和文档。
- **App：`/Users/joo00der/.codex/worktrees/e053/key-port/dist/KeyPortDesignPreview.app`**。
- 构建：在该 worktree 执行 `./script/build_design_preview.sh`。SwiftPM 独立预览 executable；本次构建 2.71 秒，ad-hoc codesign 验证通过，实际 App 已启动并操作。
- [构建日志](evidence/6db2877/build.log)、[测试日志](evidence/6db2877/tests.log)、[图像/AX/二进制哈希清单](evidence/6db2877/manifest.json)。
- **[evidence/6db2877](evidence/6db2877) 是上一版完整流程证据。** 中间构建的截图已从本阶段目录删除。原 #90 证据只证明其历史版本。

打开此 App → Cmd+N（或主页“+”）→ 使用固定示例数据提交 → 按需确认占位主机指纹 → 通过页脚“模拟完成”或 Cmd+J 逐次回传结果。右下“模拟场景”可选择各错误状态；同一面板中切换为“首次连接成功”再重试可验证恢复。重新打开面板重置 fixture。不要输入真实密码。

## 逐项结论与证据

下表“已验收”仅指本阶段代理实际执行并确认的隔离行为；整体视觉、新增页面设计和前置验收仍需用户答复。

| 环节 | 结论 | 最终证据（同名 `.jpg` + `.ax.txt`） |
|---|---|---|
| 实际设计读取 | 已验收 | Figma design context 16:2、17:34、16:9、17:2、17:18、16:23、16:16、20:26、20:49；未用 10:* |
| 原表单衔接 | 已验收 | form-tab-address、invalid-address：复用 AccessFormView；`!!!` 留在表单且保留输入；没有第二套表单 |
| 一次密码填写完成流程 | 已验收 | password-mode-success；本次默认示例密码提交后连续完成主机确认/登录/授权/验证 |
| 已有密钥模式 | 已验收 | existing-key-form；无密码可提交，不无条件认为设备已授权 |
| 条件主机确认 | 已验收 | host-confirmation、trusted-login；确认后重试不再重复弹确认；地址、算法、完整演示指纹可见 |
| 身份不匹配停止 | 已验收 | failure-identity-mismatch；没有忽略/继续/重新验证入口；模型测试也阻断重试事件 |
| 主机取消保留输入 | 已验收 | host-cancel-form；密码为空，非敏感字段保留 |
| 连续三步进度 | 已验收 | login、authorization、verification；同一 880×740 面板更新，无长向导重复填写 |
| 登录/授权/验证取消 | 已验收 | login-cancel-form、authorization-cancelled、verification-cancelled；分别保留未执行/结果未知/已授权未验证语义 |
| 不可达与认证失败 | 已验收 | failure-unreachable、failure-login；未开始授权，没有显示免密已配置 |
| 授权失败与未知结果 | 已验收 | failure-authorization、failure-authorization-unknown；保守说明可能有副作用，重试先核对 |
| 已授权但验证失败 | 已验收 | failure-verification；明确部分成功，不显示全部成功 |
| 失败/取消重试 | 已验收 | retry-skips-install、unknown-retry-skips-install、recovered-success；实际重试从登录直接进入验证，无重复安装等待 |
| 修改地址复用账户授权 | 已验收 | new-address-form（`2001:db8::1`）、new-address-login、new-address-skips-install；同一 fixture 服务器账户已授权后改地址不重复安装；模型测试另覆盖账户/设备/服务器隔离 |
| 密码清理与表单恢复 | 已验收 | password-retry-cleared、partial-edit-preserves-form；无密码隐式重用，恢复时不自动出现必填错误 |
| 迟到回调 | 已验收（状态测试） | 非合作 adapter 模拟每一步取消后仍返回；旧结果不能推进状态；取消安装后实际迟到写入，重试查询后不重复安装 |
| 成功/终端/复制反馈 | 已验收 | success、terminal-pending、terminal-feedback、copy-feedback；没有实际终端/剪贴板副作用，只有模拟回调后显示完成 |
| 终端不可用/复制失败恢复 | 已验收 | terminal-unavailable、copy-failed、copy-recovered；保留配置成功状态并提供重试、复制、可选命令文本 |
| 原生键盘与 AX | 已验收 | Cmd+N 打开；Tab 从名称到地址；Return 提交；host-return-blocked 证明 Return 不误确认；Escape 在三个步骤执行相应取消；Cmd+J 返回 fixture 结果；所有主要状态都有 AX |
| 页面基础布局与可见性 | 已验收（自验范围） | 实际截图检查主要控件、文字、色彩角色与面板边界；修正进度完成颜色、成功页行高与失败页 20/21/18 点行高 |
| 原尺度像素/系统字形观感 | 待用户验收 | 工具缩放及原生字形限制如下；不声明像素级一致 |
| 新增错误/取消设计 | 待用户验收 | Figma 未覆盖项逐项列在下一表；交互已自验，设计选择待确认 |

## 与设计逐项对照

| Figma | 最终实现与差异 | 结论 |
|---|---|---|
| 16:2 单表单 | 沿用 #91 的 880×740、32 外边距、816×464 主内容、137/112 主操作；只添加恢复提示和页脚模拟控制 | 结构/交互已验收；承接 #90 原生字形待确认 |
| 17:34 首次确认 | 保留 600×360 白卡、28 内边距、21 点标题、122/112 按钮；嵌入同一 880×740 面板以保持流程连续。新增一行完整演示指纹，原设计仅占位说明。未设置 Return 默认确认 | 新增完整指纹与承载位置待用户验收；行为已验收 |
| 16:9、17:2、17:18 进度 | 88 高卡片、14 间距；第一步文字蓝色，后两步进行中详情蓝色。第二步明确“先核对、仅缺失才安装”，验证页说明已有授权不重复安装 | 结构已验收；安全语义新增文案待用户验收 |
| 17:2、17:18 空操作区 | 新增“停止后续步骤”，之后显示取消页及授权残留说明；不暗示回滚。页脚增加手动完成模拟控制，没有定时成功 | 新增待用户验收；取消与回调行为已验收 |
| 16:23 登录失败 | 保留警告图标、20 点标题、主按钮/次按钮、文字层级。解释改为“账户或凭据”，兼容现有密钥；本次授权状态单独显示，防止重试已有授权时谎称未安装 | 行高偏差已修；语义文案待用户验收 |
| 无对应节点：不可达、身份不匹配、授权失败、授权未知、验证失败、取消 | 复用失败页语言和间距；分别说明未执行/结果不确定/部分成功；身份不匹配移除重试入口 | 新增待用户验收；功能已验收 |
| 16:16 成功 | 30 图标位、22/33 技术标题、14/21 成功说明、17/26 命令、12/18 注释、112×38 主按钮。明确“模拟”，不照抄“服务器详情和拓扑随之更新”的未实现承诺 | 结构已验收；模拟措辞待用户验收 |
| 20:26、20:49 交接 | 同页 #ECF4FF 轻反馈、命令可选、重试/复制入口；全部完成依赖适配结果；模拟器不打开终端或写剪贴板 | 已验收（模拟交互）；原生视觉待用户验收 |
| 无对应节点：终端/复制失败 | 继续沿用同页蓝色反馈区，清晰说明失败与恢复；不重新执行授权流程 | 新增待用户验收 |
| 字体/图标 | 复用 #91 系统中文、SF Symbols、打包 JetBrains Mono。Figma 原生风格约定 3:528 允许该映射；勾选/警告符号语义与结构匹配，抗锯齿和字重观感有差异 | 待用户确认整体观感；不声称逐像素一致 |

## 截图尺度说明

共 37 组截图与 AX。所有 JPEG 都是 `sky.get_app_state` 返回的原始字节，未裁剪、放大、重新采样、改图或格式转换。清单记录每张文件的实际尺寸和哈希。

- App 主页内容参数 1320×820，工具返回主页 1237×768。延续 #90 已识别的缩放限制。
- 面板参数 880×740，工具返回 JPEG 容器 880×740。进度/失败/成功状态多数为面板填满画面。
- **表单和主机确认的若干工具图包含被缩小的父窗口与 sheet，以及底部白区。即使文件尺寸为 880×740，也不能把它说成 1:1 面板截图。** 这些图仅用于内容/交互/结构核对。工具没有请求原尺寸的参数，未用放大修补。请在真实 App 上检查这两类状态的原尺寸布局。
- Figma 参考设计通过实际 design context 读取；#90 原有 Figma 导出位于其证据目录。未伪造本阶段原尺寸对照图。

## 验证范围

最终源码 `6db2877` 的 `swift test --filter KeyPortInterfaceTests`：**17/17 通过**，其中原表单 4 项、首次访问状态 13 项。覆盖身份确认/阻断、失败不误报、状态未知不安装、各步取消/迟到、账户设备服务器维度、新地址复用授权、部分成功重试、凭据重输、实际回调决定终端/复制结果。日志中其他 target 的 0 selected tests 不是其全量通过。本阶段不声称重跑整个生产测试套件。

最终 build、codesign strict verify、当前 worktree 实际进程、原生点击/键盘/截图/AX 已验证。`git diff --check` 通过。没有真实 SSH、真实服务器、真实密码、钥匙串、CloudKit、生产配置写入。此前发现的编译初始化次序问题、取消提示文案和失败页行高已修复；没有留下已确认而未修复的功能问题。

## 需用户实际答复的验收

1. 在上述 App 用 Cmd+N 打开表单，提交后查看 600×360 主机确认卡，核对原尺寸、中文/技术字形与承载位置。
2. 右下模拟场景依次查看登录失败、授权失败、授权结果未知、已授权但验证失败；进度按 Escape 查看取消页。确认新增状态的提示和恢复操作符合预期。
3. 成功后查看终端/复制模拟反馈。确认本阶段整体视觉；同时解决 #90 / PR #91 留下的主页原尺度与字形验收。

收到实际确认前保持堆叠草稿。若任一项未通过，继续修复此阶段；不进入 Graph/真实接入，不关闭总任务。
